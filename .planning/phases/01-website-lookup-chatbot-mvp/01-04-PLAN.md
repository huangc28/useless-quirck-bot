---
phase: 01-website-lookup-chatbot-mvp
plan: "04"
type: execute
wave: 3
depends_on: ["01-02", "01-03"]
files_modified:
  - internal/telegram/telegram.go
  - internal/telegram/telegram_test.go
  - internal/app/app.go
  - internal/app/app_test.go
  - cmd/server/main.go
  - README.md
  - .env.example
autonomous: true
requirements: ["REQ-001"]
user_setup:
  - "For live Telegram demo, user must provide TELEGRAM_BOT_TOKEN and a reachable HTTPS webhook URL."
  - "For live browser lookup demo, user must configure LOOKUP_WORKER_CMD to a local agent command with Chrome MCP access."
must_haves:
  truths:
    - "D-19: Primary demo path supports live Telegram, live LLM router, live lookup worker, and Telegram reply."
    - "D-20: Mock fallback mode remains documented and runnable."
    - "D-21: README documents demo messages 你好, 請問義美小泡芙多少錢, and 請問 momo 義美小泡芙多少錢."
    - "D-22: Live failures produce graceful status and never silently fake success."
    - "SPEC-R1: Telegram webhook payloads can be parsed and answered with the correct chat ID."
    - "SPEC-R3: general_chat returns a reply without invoking lookup workers or lookup cache."
    - "SPEC-R4 SPEC-R5: public_lookup and browser_lookup route through the correct worker/cache behavior."
    - "SPEC-R6: auth_required produces a browser-session preparation reply, not credential collection."
  artifacts:
    - "cmd/server/main.go exists."
    - "internal/app/app.go exists."
    - "internal/telegram/telegram.go exists."
    - "README.md exists."
  key_links:
    - "Telegram handler calls app orchestrator."
    - "App orchestrator calls router, cache, worker, chat responder, and Telegram sender interfaces."
---

<objective>
Wire Telegram ingress, message orchestration, reply formatting, server startup, and demo documentation.

Purpose: Turn the internal router/cache/worker pieces into the actual chatbot MVP that can be run live or with mock fallback.
Output: HTTP server, Telegram client/handler, app orchestrator, integration tests, README demo guide, and updated env template.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-website-lookup-chatbot-mvp/01-SPEC.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-RESEARCH.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-VALIDATION.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-02-SUMMARY.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-03-SUMMARY.md
@internal/contracts/contracts.go
@internal/router/router.go
@internal/chat/chat.go
@internal/cache/cache.go
@internal/worker/worker.go
</context>

<threat_model>
Threats covered by this plan:
- T-04-01 high: Telegram token leaked in logs/docs. Mitigation: README and code use env var names only; tests must not include real tokens.
- T-04-02 high: auth wall prompts user to submit credentials in chat. Mitigation: auth_required reply asks for browser session preparation only.
- T-04-03 medium: live lookup failure is hidden by mock answer. Mitigation: live failure path returns explicit graceful status.
- T-04-04 medium: malformed Telegram payload panics server. Mitigation: handler validates message text and chat ID before orchestration.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement Telegram webhook parsing and sender</name>
  <files>internal/telegram/telegram.go, internal/telegram/telegram_test.go</files>
  <read_first>.planning/phases/01-website-lookup-chatbot-mvp/01-RESEARCH.md, internal/contracts/contracts.go</read_first>
  <action>Create package `internal/telegram` with structs for the subset of Telegram update JSON needed by Phase 1: update.message.chat.id and update.message.text. Implement `ParseUpdate(io.Reader) (chatID int64, text string, ok bool, err error)`. Implement `Sender` interface with `SendMessage(ctx context.Context, chatID int64, text string) error` and `HTTPSender` that posts JSON to `https://api.telegram.org/bot{token}/sendMessage` with `chat_id` and `text`. Tests must use `httptest.Server` for sender behavior and fixture JSON for webhook parsing.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `internal/telegram/telegram.go` contains `func ParseUpdate`.
    - `internal/telegram/telegram.go` contains `type Sender interface`.
    - `internal/telegram/telegram.go` contains `sendMessage`.
    - `internal/telegram/telegram_test.go` contains `"chat":{"id":`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Telegram payloads can be parsed and replies can be sent through an interface.</done>
</task>

<task type="auto">
  <name>Task 2: Implement app orchestration and reply formatting</name>
  <files>internal/app/app.go, internal/app/app_test.go</files>
  <read_first>internal/router/router.go, internal/chat/chat.go, internal/cache/cache.go, internal/worker/worker.go, internal/contracts/contracts.go, .planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md</read_first>
  <action>Create package `internal/app` with `App` struct depending on router client, chat responder, cache store, worker client, clock function, and config. Implement `HandleMessage(ctx, text string) (reply string, err error)`. Behavior: `general_chat` calls chat responder only; low confidence returns clarification; router failure returns safe fallback; lookup modes check force refresh and cache; fresh cache returns cached answer with source/time text; cache miss or force refresh calls worker with mode-specific timeout; worker `ok` replies render answer text plus at least one evidence source/title/link and observed/query time in the Telegram reply; worker `no_result` is cacheable using the no_result TTL; worker `error`, subprocess error, timeout, and invalid worker JSON are not cached and return a graceful failure/status reply when no stale cache exists; expired cache plus worker error/subprocess error/timeout/invalid JSON returns stale result with expired labeling; none of the live failure paths may silently substitute mock success; `auth_required` returns browser-session preparation message and does not ask for credentials.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `internal/app/app.go` contains `func (a *App) HandleMessage`.
    - `internal/app/app_test.go` contains `general_chat`.
    - `internal/app/app_test.go` contains `stale`.
    - `internal/app/app_test.go` contains `auth_required`.
    - `internal/app/app_test.go` contains `不要快取`.
    - `internal/app/app_test.go` contains `observed_at`.
    - `internal/app/app_test.go` contains `worker_error`.
    - `internal/app/app_test.go` contains `invalid JSON`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>App orchestrator implements the complete mode/cache/worker behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add HTTP server entrypoint</name>
  <files>cmd/server/main.go, .env.example</files>
  <read_first>internal/config/config.go, internal/telegram/telegram.go, internal/app/app.go, scripts/mock_lookup_worker.sh</read_first>
  <action>Create `cmd/server/main.go` that loads config, initializes cache at `CACHE_PATH`, selects mock or live router/chat/worker based on `LLM_MODE` and `LOOKUP_WORKER_MODE`, registers `POST /telegram/webhook`, parses Telegram updates, calls `App.HandleMessage`, and sends replies through Telegram sender. If `LOOKUP_WORKER_MODE=mock` and `LOOKUP_WORKER_CMD` is empty, default to `./scripts/mock_lookup_worker.sh`. Keep startup logs free of token values. Update `.env.example` with comments showing `LLM_MODE=mock|live` and `LOOKUP_WORKER_MODE=mock|live`.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `cmd/server/main.go` contains `/telegram/webhook`.
    - `cmd/server/main.go` contains `LOOKUP_WORKER_MODE`.
    - `cmd/server/main.go` does not contain an example real Telegram token.
    - `.env.example` contains `LLM_MODE=mock`.
    - `.env.example` contains `LOOKUP_WORKER_CMD=`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Server can run the webhook orchestration with mock fallback or live configuration.</done>
</task>

<task type="auto">
  <name>Task 4: Write README demo and verification guide</name>
  <files>README.md</files>
  <read_first>.planning/phases/01-website-lookup-chatbot-mvp/01-SPEC.md, .planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md, .planning/phases/01-website-lookup-chatbot-mvp/01-RESEARCH.md, .env.example</read_first>
  <action>Create `README.md` with sections `## Overview`, `## Architecture`, `## Environment`, `## Run Locally`, `## Telegram Webhook`, `## Mock Fallback`, `## Live Lookup Worker`, `## Demo Script`, `## Cache Behavior`, `## Auth Wall Behavior`, and `## Verification`. The demo script must list exactly these three chat messages: `你好`, `請問義美小泡芙多少錢`, `請問 momo 義美小泡芙多少錢`. The live lookup worker section must explain that `LOOKUP_WORKER_CMD` can point to a local agent command such as Codex CLI using an output schema, and that browser lookup requires Chrome MCP/browser profile setup. The auth wall section must state that users should not send third-party credentials in Telegram.</action>
  <verify><automated>go test ./... && rg \"請問義美小泡芙多少錢|請問 momo 義美小泡芙多少錢|Auth Wall Behavior|Mock Fallback\" README.md</automated></verify>
  <acceptance_criteria>
    - `README.md` contains `## Demo Script`.
    - `README.md` contains `你好`.
    - `README.md` contains `請問義美小泡芙多少錢`.
    - `README.md` contains `請問 momo 義美小泡芙多少錢`.
    - `README.md` contains `## Auth Wall Behavior`.
    - `README.md` contains `do not send third-party credentials`.
  </acceptance_criteria>
  <done>Reviewer can understand and run live or mock demo flows from README.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` exits 0.
- [ ] README contains the three locked demo messages.
- [ ] Server route `/telegram/webhook` is implemented.
- [ ] Auth-required behavior is tested and documented.
- [ ] Mock fallback and live setup are both documented.
</verification>

<success_criteria>
- Telegram webhook can parse a message and send a reply.
- App orchestration exercises general chat, public lookup, browser lookup, cache, and auth-required paths.
- README documents live-primary demo and mock fallback.
- The implementation does not ask users for third-party credentials in Telegram.
</success_criteria>

<output>
After completion, create `.planning/phases/01-website-lookup-chatbot-mvp/01-04-SUMMARY.md`
</output>
