---
phase: 01-website-lookup-chatbot-mvp
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - internal/router/router.go
  - internal/router/router_test.go
  - internal/chat/chat.go
  - internal/chat/chat_test.go
autonomous: true
requirements: ["REQ-001"]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04: Router returns Minimal + cache_hint fields and never an execution plan."
    - "D-05: Confidence below 0.65 returns clarification behavior."
    - "D-06: Router errors and invalid JSON do not invoke lookup."
    - "D-07: Normalized question preserves original language, site names, and URLs."
    - "D-18: general_chat bypasses lookup cache."
  artifacts:
    - "internal/router/router.go exists."
    - "internal/chat/chat.go exists."
  key_links:
    - "Router output uses internal/contracts.RouterResult."
    - "General chat response path can be used by orchestration without lookup."
---

<objective>
Implement the LLM intent router and general-chat response path with deterministic mock mode and strict validation.

Purpose: Ensure every Telegram message can be classified before execution, and general chat can be answered without cache or lookup worker calls.
Output: Router package, chat responder package, and tests for locked routing fixtures.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-website-lookup-chatbot-mvp/01-SPEC.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-RESEARCH.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-01-SUMMARY.md
@internal/contracts/contracts.go
@internal/config/config.go
</context>

<threat_model>
Threats covered by this plan:
- T-02-01 high: LLM output tries to trigger unsupported modes. Mitigation: enum validation rejects unknown modes.
- T-02-02 high: model guesses volatile facts in public lookup. Mitigation: router only classifies; it never returns final lookup answers.
- T-02-03 medium: low-confidence classification causes unwanted tool execution. Mitigation: below-threshold output becomes a clarification response.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Implement router validation and mock router</name>
  <files>internal/router/router.go, internal/router/router_test.go</files>
  <read_first>internal/contracts/contracts.go, internal/config/config.go, .planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md</read_first>
  <action>Create package `internal/router`. Define interface `Client` with `Route(ctx context.Context, message string) (contracts.RouterResult, error)`. Implement `NormalizeQuestion(message string) string` that trims whitespace and removes leading `/ask`, `/price`, and `/lookup` command prefixes while preserving language, site names, and URLs. Implement `ValidateResult(result contracts.RouterResult, threshold float64) (contracts.RouterResult, RouteDecision)` where `RouteDecision` can represent executable route, clarification, or safe fallback. Implement `MockClient` with deterministic routing for `你好` -> `general_chat`, `請問義美小泡芙多少錢` -> `public_lookup` with cache_hint `{lookup_type:\"price\", target:\"義美小泡芙\"}`, `請問 momo 義美小泡芙多少錢` -> `browser_lookup` with site `momo`, and URL-containing messages -> `browser_lookup` with url populated.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `internal/router/router.go` contains `type Client interface`.
    - `internal/router/router.go` contains `func NormalizeQuestion`.
    - `internal/router/router.go` contains `type MockClient`.
    - `internal/router/router_test.go` contains `請問義美小泡芙多少錢`.
    - `internal/router/router_test.go` contains `browser_lookup`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Router can classify locked fixtures and reject unsafe output.</done>
</task>

<task type="auto">
  <name>Task 2: Add live-router adapter behind the same interface</name>
  <files>internal/router/router.go, internal/router/router_test.go</files>
  <read_first>schemas/router_result.schema.json, internal/config/config.go, .planning/phases/01-website-lookup-chatbot-mvp/01-RESEARCH.md</read_first>
  <action>Add `LiveClient` to `internal/router` using `net/http`. It should be constructed with API key, model, HTTP client, and endpoint string. The live prompt must instruct the model to return only the router schema fields and explicitly state that it must not answer user lookup questions. The implementation must parse JSON into `contracts.RouterResult` and pass it through the same validator as mock mode. If API key or model is missing, return an error that orchestration can convert into safe fallback. Do not hard-code a final OpenAI model; read `OPENAI_MODEL` from config and allow empty model to fail clearly in live mode.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `internal/router/router.go` contains `type LiveClient`.
    - `internal/router/router.go` contains `OPENAI` nowhere; config owns env names.
    - `internal/router/router.go` contains `must not answer`.
    - `internal/router/router_test.go` includes an invalid JSON test.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Live router can be configured without changing orchestration code.</done>
</task>

<task type="auto">
  <name>Task 3: Implement general chat responder with mock and live modes</name>
  <files>internal/chat/chat.go, internal/chat/chat_test.go</files>
  <read_first>internal/contracts/contracts.go, internal/config/config.go, .planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md</read_first>
  <action>Create package `internal/chat` with interface `Responder{Respond(ctx context.Context, question string) (string, error)}`. Implement `MockResponder` that replies to `你好` with a concise Traditional Chinese message saying it can help search website data, including example `請問義美小泡芙多少錢`. Implement `LiveResponder` using a configurable HTTP client and API key/model, but keep it behind the interface. General chat must not invoke lookup or cache packages.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `internal/chat/chat.go` contains `type Responder interface`.
    - `internal/chat/chat.go` contains `type MockResponder`.
    - `internal/chat/chat_test.go` contains `請問義美小泡芙多少錢`.
    - `internal/chat/chat_test.go` asserts the mock response is non-empty.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>General chat has a safe response path that does not touch lookup/cache.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` exits 0.
- [ ] Router fixtures classify all locked demo messages correctly.
- [ ] Invalid router output cannot produce an executable lookup route.
- [ ] General chat responder returns a message without cache/worker dependencies.
</verification>

<success_criteria>
- Router mode decisions are deterministic in mock mode.
- Live mode is isolated behind interfaces.
- Low confidence and invalid router output fail safe.
- General chat path is implemented separately from lookup paths.
</success_criteria>

<output>
After completion, create `.planning/phases/01-website-lookup-chatbot-mvp/01-02-SUMMARY.md`
</output>
