---
phase: 01-website-lookup-chatbot-mvp
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - .gitignore
  - .env.example
  - schemas/router_result.schema.json
  - schemas/lookup_request.schema.json
  - schemas/lookup_response.schema.json
  - internal/contracts/contracts.go
  - internal/contracts/contracts_test.go
  - internal/config/config.go
  - internal/config/config_test.go
autonomous: true
requirements: ["REQ-001"]
user_setup: []
must_haves:
  truths:
    - "D-01 D-02 D-03 D-04: Router contract types include mode, confidence, reason, normalized_question, and optional cache_hint."
    - "D-08 D-10 D-11: Lookup worker contract types include mode-specific requests and strict status-bearing responses."
    - "D-05: Config exposes ROUTER_CONFIDENCE_THRESHOLD with default 0.65."
    - "D-12: Config exposes public/browser lookup timeout defaults of 20s and 60s."
    - "D-19 D-20: Config supports live-primary runtime and mock fallback modes."
  artifacts:
    - "go.mod exists and declares a Go module."
    - "schemas/router_result.schema.json exists."
    - "schemas/lookup_request.schema.json exists."
    - "schemas/lookup_response.schema.json exists."
    - "internal/contracts/contracts.go exists."
    - "internal/config/config.go exists."
  key_links:
    - "JSON schema files match the Go contract field names."
---

<objective>
Create the Go project foundation, shared contracts, JSON schemas, and configuration surface that every later plan depends on.

Purpose: Make the routing, lookup, cache, and Telegram layers agree on stable data shapes before any orchestration code is written.
Output: Go module, ignored runtime files, env template, contract types, schema files, and config loader tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-SPEC.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-RESEARCH.md
@.planning/phases/01-website-lookup-chatbot-mvp/01-VALIDATION.md
</context>

<threat_model>
Threats covered by this plan:
- T-01-01 high: secrets accidentally committed. Mitigation: `.gitignore` and `.env.example` must exclude real `.env` and runtime DB files.
- T-01-02 medium: invalid LLM/worker modes trigger tools. Mitigation: contract constants and schema enums define the only accepted modes/statuses.
- T-01-03 medium: timeout/config mistakes cause unbounded subprocesses. Mitigation: config defaults encode public/browser timeouts.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Go module and runtime hygiene files</name>
  <files>go.mod, .gitignore, .env.example</files>
  <read_first>.planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md, .planning/phases/01-website-lookup-chatbot-mvp/01-RESEARCH.md</read_first>
  <action>Create `go.mod` with module path `interview-chatbot` and Go version compatible with the local Go toolchain. Create `.gitignore` entries for `.env`, `.env.local`, `data/`, `*.db`, `*.db-shm`, `*.db-wal`, and local build outputs. Create `.env.example` with keys `SERVER_ADDR=:8080`, `PUBLIC_BASE_URL=`, `TELEGRAM_BOT_TOKEN=`, `TELEGRAM_WEBHOOK_SECRET=`, `LLM_MODE=mock`, `OPENAI_API_KEY=`, `OPENAI_MODEL=`, `LOOKUP_WORKER_MODE=mock`, `LOOKUP_WORKER_CMD=`, `CACHE_PATH=data/cache.db`, `ROUTER_CONFIDENCE_THRESHOLD=0.65`, `PUBLIC_LOOKUP_TIMEOUT=20s`, and `BROWSER_LOOKUP_TIMEOUT=60s`.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `go.mod` contains `module interview-chatbot`.
    - `.gitignore` contains `.env`.
    - `.gitignore` contains `data/`.
    - `.env.example` contains `ROUTER_CONFIDENCE_THRESHOLD=0.65`.
    - `.env.example` contains `PUBLIC_LOOKUP_TIMEOUT=20s`.
    - `.env.example` contains `BROWSER_LOOKUP_TIMEOUT=60s`.
  </acceptance_criteria>
  <done>Go module and runtime hygiene files exist with no committed secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Define shared contract types and schemas</name>
  <files>internal/contracts/contracts.go, internal/contracts/contracts_test.go, schemas/router_result.schema.json, schemas/lookup_request.schema.json, schemas/lookup_response.schema.json</files>
  <read_first>.planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md, .planning/phases/01-website-lookup-chatbot-mvp/01-SPEC.md</read_first>
  <action>Create package `internal/contracts` with string types/constants for `ModeGeneralChat`, `ModePublicLookup`, `ModeBrowserLookup`, `StatusOK`, `StatusNoResult`, `StatusAuthRequired`, and `StatusError`. Define structs `CacheHint{LookupType, Target, Site, URL string}`, `RouterResult{Mode string, Confidence float64, Reason string, NormalizedQuestion string, CacheHint *CacheHint, ForceRefresh bool}`, `LookupRequest{Mode string, Question string, NormalizedQuestion string, CacheHint *CacheHint, ForceRefresh bool}`, `Evidence{Title, URL, Source, Snippet, ObservedAt string}`, and `LookupResponse{Status string, Answer string, Evidence []Evidence, ObservedAt string, Error string}`. Add validation helpers `ValidateMode`, `ValidateStatus`, and `RouterResult.Valid(threshold float64) error`. Create JSON schemas using the same field names in snake_case JSON form: `mode`, `confidence`, `reason`, `normalized_question`, `cache_hint`, `force_refresh`, `status`, `answer`, `evidence`, `observed_at`, and `error`.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `internal/contracts/contracts.go` contains `const ModeGeneralChat`.
    - `internal/contracts/contracts.go` contains `type CacheHint struct`.
    - `internal/contracts/contracts.go` contains `func ValidateMode`.
    - `schemas/router_result.schema.json` contains `"general_chat"`.
    - `schemas/lookup_response.schema.json` contains `"auth_required"`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Shared Go structs and JSON schemas encode all locked router/worker modes and statuses.</done>
</task>

<task type="auto">
  <name>Task 3: Implement environment configuration loader</name>
  <files>internal/config/config.go, internal/config/config_test.go</files>
  <read_first>internal/contracts/contracts.go, .env.example, .planning/phases/01-website-lookup-chatbot-mvp/01-CONTEXT.md</read_first>
  <action>Create package `internal/config` with `Config` fields for server address, public base URL, Telegram token, webhook secret, LLM mode, OpenAI API key, OpenAI model, lookup worker mode, lookup worker command, cache path, router threshold, public lookup timeout, and browser lookup timeout. Implement `LoadFromEnv() Config` using defaults from `.env.example`. Parse durations with `time.ParseDuration`; invalid durations must fall back to `20s` for public and `60s` for browser. Add tests that clear/set environment variables with `t.Setenv` and verify defaults and overrides.</action>
  <verify><automated>go test ./...</automated></verify>
  <acceptance_criteria>
    - `internal/config/config.go` contains `RouterConfidenceThreshold`.
    - `internal/config/config.go` contains `PublicLookupTimeout`.
    - `internal/config/config.go` contains `BrowserLookupTimeout`.
    - `internal/config/config_test.go` contains `0.65`.
    - `internal/config/config_test.go` contains `60*time.Second`.
    - `go test ./...` exits 0.
  </acceptance_criteria>
  <done>Config loader exposes all runtime switches needed by later plans.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `go test ./...` exits 0.
- [ ] `.env.example` documents all required runtime variables.
- [ ] Contract schemas and Go structs contain the same router/worker field names.
</verification>

<success_criteria>
- Go module exists.
- Runtime secrets and cache files are ignored.
- Router and lookup contracts are represented in Go and JSON Schema.
- Config defaults match locked decisions from CONTEXT.md.
</success_criteria>

<output>
After completion, create `.planning/phases/01-website-lookup-chatbot-mvp/01-01-SUMMARY.md`
</output>
